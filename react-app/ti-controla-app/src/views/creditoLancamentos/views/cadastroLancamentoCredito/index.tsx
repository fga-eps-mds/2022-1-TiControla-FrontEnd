import { RootStackScreenProps } from '../../../../application/types/RootStack';
export default function CadastroLancamentoCredito({ navigation, route }: RootStackScreenProps<"CadastroLancamentoCredito">) {
  const {id} = route.params;
  
  return <>
    
  </>
}